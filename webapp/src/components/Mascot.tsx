// Mascot đã bị gỡ khỏi dự án theo yêu cầu (không còn nơi nào import file này).
// Giữ file rỗng thay vì xoá vì sandbox không xoá được file trên mount này;
// bạn có thể xoá thủ công file này (và thư mục public/mascot/) ở máy thật.
export {};
